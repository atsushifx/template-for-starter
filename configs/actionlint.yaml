# actionlint configuration
# Documentation: https://github.com/rhysd/actionlint/blob/main/docs/config.md

# Self-hosted runner configuration
self-hosted-runner:
  # Custom runner labels
  labels:
    - ubuntu-slim

# Configuration variables defined in repository or organization
# Set to empty array to disallow all config variables, or null to disable check
config-variables: null
